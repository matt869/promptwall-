# Normalize to LF in the repo; check out native on Windows.
* text=auto eol=lf

# Never let CRLF reach files that a Linux container executes.
*.sh   text eol=lf
*.yaml text eol=lf
*.yml  text eol=lf
Dockerfile text eol=lf
Makefile   text eol=lf

# Binary fixtures
*.pdf  binary
*.onnx binary
